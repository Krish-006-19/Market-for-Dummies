# Base URL of the backend API (no trailing slash).
# Production:
VITE_API_URL=https://mutual-funds-1.onrender.com
# Local development (point this at your local backend instead):
# VITE_API_URL=http://localhost:5000
